{
  "name": "disk-digest",
  "version": "1.0.0",
  "type": "module",
  "description": "Slack bot that scrapes arXiv astro-ph daily, filters for protoplanetary-disk papers with Claude, and posts a summarized digest.",
  "scripts": {
    "start": "node --env-file=.env disk-digest.js",
    "dry-run": "node --env-file=.env disk-digest.js --dry-run"
  },
  "engines": {
    "node": ">=20.6"
  },
  "author": "Richard Teague",
  "license": "ISC",
  "dependencies": {
    "openai": "^6.44.0"
  }
}
